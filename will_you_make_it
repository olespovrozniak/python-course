def zero_fuel(distance_to_pump, mpg, fuel_left):
    result = True
    if distance_to_pump / mpg > fuel_left:
        result = False
        print(result)
    else:
        print(result)
    return(result)